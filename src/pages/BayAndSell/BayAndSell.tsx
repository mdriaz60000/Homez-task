import main from "../../assets/Section → Main → Section (1).png"

const BayAndSell = () => {
    return (
        <div>
            <img src={main} alt="" />
        </div>
    );
};

export default BayAndSell;